S1(config)# interface <int id>
S1(config-if)# spanning-tree portfast - enable portfast, so that port goes into forwarding state immediately
S1(config-if)# spanning-tree bpduguard enable - enable BPDU guard, to avoid from forwarding BPDU